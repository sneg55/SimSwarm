import { describe, it, expect } from 'vitest'

// Tests split across multiple files to stay under 300-line limit.
// See: useSimulationData.spec.js, useScrollReveal.spec.js,
//      useTooltipPosition.spec.js, useResultsExport.spec.js
describe('composables index', () => {
  it('is split across multiple files', () => {
    expect(true).toBe(true)
  })
})

# Report tests are split across focused modules:
#   report_fixtures.py          — shared fixtures (make_result, SAMPLE_MARKDOWN)
#   test_report_tools.py        — ReportTools tests (get_top_posts, get_coalitions, etc.)
#   test_report_generator.py    — ReportGenerator and Report dataclass tests
